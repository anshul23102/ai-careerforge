{
  "name": "ai-careerforge-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=@ai-careerforge/shared --workspace=@ai-careerforge/parsers",
    "test": "npm run test --workspaces --if-present"
  }
}
